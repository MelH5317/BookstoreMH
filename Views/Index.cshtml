@page
@model IndexModel
@{
    ViewData["Title"] = "Menus";
}

<div class="table-responsive">
    <table class="table table-striped recipe">
        <thead>
            <tr>
                <th>Book Title</th>
                <th>Author</th>
                <th>Price</th>
                <th>   </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var recipe in Model.Books)
            {
            <tr>
                <td class="align-middle">@Book.Author</td>
                <td class="align-middle">@Book.Title</td>
                <td class="align-middle">@Book.Price</td>
                <td class="align-middle"><a asp-page="Books/View" asp-route-id="@book.BookId" class="btn btn-primary">View</a></td>
            </tr>\\
            }
        </tbody>
    </table>
</div>
<a asp-page="Books/Create" class="btn btn-success">Add new Book</a>